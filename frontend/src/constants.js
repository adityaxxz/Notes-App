export const ACCESS_TOKEN = "access";
export const REFRESH_TOKEN = "refresh";

// we're gonna use the local storage to store the tokens, and wanna have a key(these constants) that i can use to access my local storage. 